// https://sharing-chamois-75.clerk.accounts.dev

const authConfig = {
  providers: [
    {
      domain: "https://sharing-chamois-75.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
